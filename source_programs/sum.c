#include <stdio.h>

int func(int a, int b){
	return a*b;
}
int sfe_main(int *a, int *b, int l, int l2) {
		  int res = 0, i = 0;
		  return func(a[0], b[0]);
}

/* Driver program to test above function */
int main()
{
		  int *a, *b;
		  int la, lb;
		  int total = sfe_main(a, b, la, lb);
		  return 0;
}

